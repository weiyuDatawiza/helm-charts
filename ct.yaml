remote: origin
chart-dirs:
  - ./
target-branch: main
helm-extra-args: '--set imagePullSecrets=cred --set PROVISIONING_KEY=${{ secrets.PROVISIONING_KEY }} --set PROVISIONING_SECRET=${{ secrets.PROVISIONING_SECRET }}'